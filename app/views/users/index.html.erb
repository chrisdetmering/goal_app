<a href="<%= new_session_url %>">Log-in</a>
<p>or</p>
<a href="<%= new_user_url %>">Sign-up</a>


<h3>Users</h3>
<% User.all.each do |user| %>

 <p> <a href="<%= user_url(user) %> "> <%= user.email %> </a> </p>
<% end %>